function calculateDogAge(age) {
    var dogYears = 7*age;
    console.log("Your doggie is " + dogYears + " years old in dog years!");
    // document.getElementById("demo").innerHTML = calculateDogAge();
}

calculateAge(2);
calculateAge(5);
calculateAge(7);